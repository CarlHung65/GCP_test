1. 看目前改了哪些檔案

```
git status
```

2. 把變更加入暫存區（add）
   
    1. 全部加入

        ```bash
        git add .
        ```

    2. 只加某個檔案

        ```bash
        git add path/to/file
        ```

3. 建立提交（commit）
```
git commit -m "你的訊息（例如：update README）"
```

4. 推到 GitHub（push）
```
git push
```
